# =============================================
# Ayura AI - Python Backend Dependencies
# =============================================

# --- Core Framework ---
fastapi==0.138.1
uvicorn[standard]==0.34.0
gunicorn==26.0.0
python-multipart==0.0.32
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Databases ---

motor==3.7.0
pymongo==4.10.1

# --- Vector Database ---
chromadb==0.6.3

# --- Authentication ---
# OAuth (Google/GitHub) is hand-rolled with httpx + manual CSRF state in
# routes/auth.py, so authlib is intentionally NOT a dependency.
pyjwt==2.13.0
bcrypt==4.2.1
httpx==0.28.1

# --- AI / LLM ---
# Upgraded to the LangChain/LangGraph 1.x line (fixes the CVEs flagged by
# pip-audit). Only langchain-openai, langgraph and langsmith are imported;
# the langchain meta-package, langchain-community and langchain-google-genai
# were removed as unused (Gemini is called via google-generativeai directly).
# langchain-core and langgraph-prebuilt are pinned explicitly because the
# latest-of-each combination is mutually incompatible — this set is verified
# to import and pass the full test suite.
langchain-core==1.4.8
langchain-openai==1.3.3
langgraph==1.2.6
langgraph-prebuilt==1.1.0
langsmith==0.9.2
openai==2.44.0
google-generativeai==0.8.4

# --- ML / Data Science ---
Pillow>=10.4.0

# --- Utilities ---
python-dotenv==1.2.2
uuid6==2024.7.10
redis==5.2.1
boto3==1.35.91
sentry-sdk[fastapi]==2.19.2
arq==0.26.1

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.23.8

# --- CORS ---
# Pinned by fastapi 0.138.x; starlette 1.x clears the request-path CVEs.
starlette==1.3.1
motor

aiofiles
reportlab>=4.2.0
tenacity==8.3.0
twilio>=9.0.0
